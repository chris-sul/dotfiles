- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc 
    ~/.p10k.zsh: p10k.zsh
    ~/.tmux.conf: tmux/tmuxconf
    ~/.tmux.session: tmux/tmuxsession
    ~/.gitconfig: gitconfig
