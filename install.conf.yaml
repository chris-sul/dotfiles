- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.vimrc: vim/.vimrc
    ~/.tmux.conf: tmux/.tmux.conf
    ~/.tmux.session: tmux/.tmux.session
    ~/.gitconfig: git/.gitconfig
    ~/.zshrc: zsh/.zshrc
